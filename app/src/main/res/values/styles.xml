<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--<style name="OneVseToolBarStyle" parent="Widget.AppCompat.ActionBar">
    <item name="ColorPrimary">@android:color/white</item>
    <item name="ColorSecondary">@android:color/white</item>
    <item name="actionMenuTextColor">@android:color/white</item>
    </style>-->


    <style name="ToolbarTitle" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">20sp</item>
    </style>
    
    <style name="AppThemeNoBar" parent="Theme.AppCompat.Light">
        <!--<item name="colorBackground">@color/colorWhite</item>
        <item name="colorText">@color/colorTheme</item>-->
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="AppThemeBar" parent="Theme.AppCompat.Light">
        <item name="android:windowNoTitle">false</item>
    </style>
    <style name="ThemeStyle" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/colorTheme</item>
    </style>

    <style name="ThemeDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@color/colorTransparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>



</resources>
